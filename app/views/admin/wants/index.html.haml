%h1 Kurs istekleri
%table{:border => "1"}
  %tr
    %td Kurs içerigi
    %td Kullanıcı e-mail
    %td Onayla
    %td İptal et

  - @wants.each do |want|
    %tr
    
      %td
      %td= want.user.email
      %td
        -if want.permission == 0
          =link_to "Onayla",   admin_wants_permit_path(want.id)
        -else
          Onaylandı
      %td
        -if want.permission == 1
          =link_to "İptal Et",   admin_wants_nopermit_path(want.id)
        -else
          İptal edildi